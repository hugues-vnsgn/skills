---
"osxsystem-skills": patch
---

Rewrite `ddd` from 3,635 words to about 1,240, with the body at or under 1,200, and promote it from `house/in-development/` to `house/discovery/` as a user-invoked skill, with a docs page, README entries, the engineer, analyst, and staff role pages, and an `ask-matt` on-ramp. Two entries, an idea or a repo to redesign, share one crunch report, every line carrying its evidence (a `path:line`, or the user's quoted words): Known, Fractured, Unknown, and a new Implicit list for rules the code enforces or the user assumes without naming. A redesign also draws today's context map beside the target. The conversation keeps the Discovery Question template and the fixed branch order, but checkpoints now ride in the next round, a one-line status strip replaces the ledger table, and one stop question inside round 3 replaces the up-front choice between a strategic and a full pass, so the short path is three replies on a repo and four on an idea. A recommendation that rests on a business fact only the user holds is flagged as a guess. The routing block, the definitions, and the prose defending the design are gone. `ARTIFACTS.md` keeps the notation and trims the pattern and stereotype definitions to name lists.
