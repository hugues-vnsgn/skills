---
"osxsystem-skills": patch
---

Promote `tdd-kmp` to the Mobile skills, with test-first guidance across shared Kotlin, Android, Compose, and SwiftUI. Route `/implement` through it for KMP/CMP apps when installed; keep `/tdd` for other code and a KMP test-seam fallback for selective installs.
