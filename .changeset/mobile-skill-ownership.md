---
"osxsystem-skills": patch
---

Separate mobile skill responsibilities and remove duplicate setup/iOS references. Restrict Ktor bearer credentials to the configured API origin, bound retries, and verify examples with executable fixtures. Refresh Android plugin/test guidance, release scope, docs and routing while preserving beta status.
